Each project has a 'board view' which shows the workflow tasks can take
To check which workflow your project is using go to Project > Project settings > workflow scheme
Issue types are allocated to different workflows


